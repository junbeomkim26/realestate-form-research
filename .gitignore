.DS_Store

# 현장 수집 결과 — 중개사 계정·매물 상세·소유자명이 담길 수 있어 기본 제외.
# 하위 폴더까지 전부 막는다. README.md 만 추적해 목록·품질 메모를 남긴다.
# 추적하려면 아래 줄을 지울 것 (단, 원격에 올릴 계획이면 그대로 두는 편이 안전).
captures/**/*.json
captures/**/*.html
captures/**/*.png

# 재현본 생성물 (7MB) — build.py 로 언제든 다시 만든다
재현/**/*_재현.html
